# Streamlit Cloud'a deploy ettiğinde dashboard ayarlarında "Secrets" kısmına
# bu içeriği yapıştır (TV_USERNAME ve TV_PASSWORD kendi değerlerinle).
#
# Streamlit Cloud, secrets.toml'i otomatik environment variable olarak yükler.
# .env dosyası repo'da olmadığı için bu yöntem zorunlu.

TV_USERNAME = "kendi_kullanici_adin"
TV_PASSWORD = "kendi_sifrelerinin"
